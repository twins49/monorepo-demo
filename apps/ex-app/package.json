{
  "name": "@monorepo-demo/ex-app",
  "version": "1.0.0",
  "description": "example app",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@monorepo-demo/utils": "workspace:*"
  }
}
